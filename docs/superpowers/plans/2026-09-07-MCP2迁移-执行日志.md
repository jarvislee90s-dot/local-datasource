# MCP 2.x 迁移 — 执行日志

- 分支：`feature/mcp2-migration`（自 main `6e13e5c` 创建；**留在本地，未推送远端**）
- 执行日期：2026-09-07 ｜ 执行方式：subagent-driven-development（每任务独立子代理/控制器 + 规格与质量两阶段审查）
- 迁移主体终版 commit：**`8bdeefbe6f5fa0a9ce5c9143e615f9de16a664af`**（docs 提交，分支第 4 个）；本日志与计划勾选随第 5 个提交入库
- 提交序列：`2b26a48`(deps) → `0afed7c`(server+契约测试) → `c38a5fb`(probe) → `8bdeefb`(docs)

## 基线（Task 0 Step 2 记录）

| 口径 | 结果 |
|---|---|
| `python -m pytest tests/ -q`（无门控） | **221 passed / 29 failed / 2 skipped**，1306s（21:46） |
| `SKIP_INTEGRATION=1 python -m pytest tests/ -q`（离线门控） | **220 passed / 32 skipped**，4.63s |
| `git rev-parse HEAD`（建分支时） | `6e13e5c1834d8010e11100964cee7be37e7d2c4d` |

29 个无门控失败均为实网集成测试（见偏差项 #0：本机系统代理失效），迁移前即如此，与本次改动无关；两口径的迁移前后对比均在相同网络条件下进行。

## 逐任务记录

### Task 0：分支、基线、依赖升级 — commit `2b26a48`

- Step 1：`git checkout main && git pull --ff-only`（Already up to date，exit 0）→ `git checkout -b feature/mcp2-migration`（成功）。
- Step 2：基线如上表。
- Step 3：`pyproject.toml:7` `mcp>=1.6.0,<2` → `mcp>=2.0.0,<3`。
- Step 4：`pip install "mcp>=2.0.0,<3"` 首跑报 `No matching distribution found` → 诊断：pip 走 Windows 系统代理 `127.0.0.1:29290`，该代理进程未运行（连接拒绝）。以 `no_proxy="*"` 绕过后 **Successfully installed mcp-2.1.1**；`python -m pip check` → `No broken requirements found`（exit 0）；`python -c "from mcp.server.mcpserver import MCPServer; print('mcp2 ok')"` → 打印 `mcp2 ok`（exit 0）。
- Step 5：离线门控 `SKIP_INTEGRATION=1 pytest tests/ -q` → **10 failed, 210 passed, 32 skipped**（≠ 基线，见偏差项 #1）；失败全部位于 `tests/test_server.py`(9) 与 `tests/test_contract_regression.py`(1)，即 spec §2 补正 2 预言的两个入口形状耦合文件（`mcp.types.Tool` 的 `inputSchema` 在 2.x 更名 `input_schema`，属性访问即 AttributeError；`build_tools()` 构造本身仍可用，故 count/names 类测试仍绿）。修复即 Task 1，按 spec 权威继续。
- Step 6：commit `2b26a48`。

### Task 1：两个入口形状耦合文件改 2.x（红） — 不提交

- Step 1：`tests/test_server.py` 全量替换为计划给定代码（`from local_datasource.server import mcp`，经 `asyncio.run(mcp.list_tools())` 断言）。
- Step 2：`tests/test_contract_regression.py`：import 换 `mcp`、新增 `_tools_by_name()`、三个 build_tools 测试换 2.x 形状、删除过时的"本文件不提交"句；CoverageError/guard/期货列名各节原样保留。
- Step 3：`pytest tests/test_server.py tests/test_contract_regression.py -q` → **2 errors during collection**（`local_datasource.server` 无 `mcp` 属性）——预期红，未提交。

### Task 2：重写 server.py（绿） — commit `0afed7c`

- Step 1-2：按计划骨架全量重写 `src/local_datasource/server.py`（16 个 `@mcp.tool(structured_output=False)` 薄函数、`_summary_for` 16 分支、`_safe_summary` 错误包装、`main()` 保留 download 分支 + `mcp.run()`）。描述文本逐字取自基线 `git show a4d2258:src/local_datasource/server.py`。
- Step 3（默认值对齐核查）：逐一对照 16 个 provider 函数签名，**全部一致，无漂移**；`tenure` 薄函数默认 `None`（provider 亦默认 None），`use_yfinance` 薄函数 `None`（分发层回退 config，显式 false 不回退——spec §4.4.2）。
- Step 4（导入冒烟）：`16 ['align_series', 'query_arxiv', 'query_bond']` ✓。
- Step 5（描述保真）：自写对照脚本（临时目录，不入库）——**16/16 工具描述逐字一致；96 个参数描述逐字一致；参数集合/enum/items/required 全部相等**；差异仅 §4.4 两类（可空参数 anyOf+default:null、use_yfinance false→null）。另发现 `query_arxiv.sort_by` 为第二个基线无 description 的参数（计划只知 `query_yfinance.period`），两处均以"无 description"渲染保真。
- Step 6：`pytest tests/test_server.py tests/test_contract_regression.py -q` → 17 passed；`SKIP_INTEGRATION=1 pytest tests/ -q` → **220 passed, 32 skipped, 0 failed**（= 基线数）。
- Step 7：commit（含 Task 1 两个文件与偏差项 #2 的 `tests/test_cli.py` 最小修复）。质量审查后 5 处 polish（注释措辞、import 归位、`_prop` 失败信息、过时任务清单清理、main docstring 措辞）并入本提交（amend → `0afed7c`）。

### Task 3：stdio 探针 + CoverageError 回归门 — commit `c38a5fb`

- Step 1：写入 `tests/test_stdio_probe.py`（计划给定内容 + 一处必要修正，见偏差项 #5：`_handshake` 的 notification 改用新增 `notify()` 只写不等应答；`test_coverage_error_wrapped_in_process` 的 monkeypatch 目标按 Task 2 偏差 #3 改为 `server._query_futures`）。
- Step 2：`python -m pytest tests/test_stdio_probe.py -v` → **6 passed in 6.47s**（首次运行曾因偏差项 #5 卡 readline，手动 echo 探针定位：服务端 initialize 应答正常，通知本就无应答）。
- Step 3：`SKIP_INTEGRATION=1 pytest tests/ -q` → **226 passed, 32 skipped**（= 基线 220 + 6）。
- Step 4：commit `c38a5fb`。

### Task 4：CLI 分支与全量验收门 — 无改动

- Step 1：`python -m local_datasource.server download --help` → 打印 usage，`exit=0`。
- Step 2：离线门控全量 → **226 passed, 32 skipped, 0 failed**；无门控全量见下节（后台补跑）。

### Task 5：文档同步 + 干净环境验收 — commit `8bdeefb`

- Step 1：CHANGELOG 顶部插入计划给定条目（逐字）。
- Step 2：`grep -in "mcp" README.md` → 无 1.x/`<2` 字样；但"扩展新数据源"节引用的 `build_tools()`/`handle_call_tool()` 已随迁移删除，按文档事实准确性同步为 2.x 形态（偏差项 #6）。
- Step 3：干净环境 `.tmp-venv` + `pip install .`（同样需 `no_proxy="*"` 绕过失效系统代理）→ `clean install ok`；echo initialize → JSON 含 `"serverInfo":{"name":"local-datasource",...}`；venv 已删除。
- Step 4：spec §6 逐条对照见下节。
- Step 5：commit `8bdeefb`；`git log --oneline main..HEAD` → 恰 4 个提交（deps/server/probe/docs），符合计划预期。

## 无门控全量（Task 4 Step 2 / Task 0 Step 5 无门控口径）

迁移后无门控 `python -m pytest tests/ -q` 结果见文末"终版核验"（与基线 221/29/2 对比）。

## 偏差项汇总（均不阻塞，已全部落地）

| # | 内容 | 理由与依据 |
|---|---|---|
| 0 | 环境：Windows 系统代理 `127.0.0.1:29290` 失效，pip/实网测试需 `no_proxy="*"` 绕过或受其影响 | 环境事实，非 spec/计划问题；基线与对比同条件，结论不受影响；Task 5 干净环境安装同法绕过 |
| 1 | 计划 Task 0 Step 5 预期"基线不变"未成立：mcp 2.x 下两入口形状耦合测试文件 10 例 AttributeError | spec §2 补正 2 明文预言（"现有 tests/ 全量通过 不是零改动"），权威顺序 spec>计划；修复即 Task 1，后续全绿 |
| 2 | （计划授权）`tests/test_cli.py::test_no_subcommand_still_starts_server` 改 patch `server.mcp.run` | 新 `main()` 直呼 `mcp.run()`，原全局 `asyncio.run` mock 结构性失效；断言语义不变（启动一次/download 分支/SystemExit(0)） |
| 3 | 计划 Task 2 骨架 bug：薄工具函数与 provider 导入同名遮蔽（骨架原样运行时 16 个工具全部自引用报 `too many values to unpack`） | 16 个 provider 改 `_` 前缀别名导入并在导入处注释说明；`_summary_for` 结构与骨架逐字一致；审查者独立复现确认骨架必然失败 |
| 4 | 计划 Task 1 测试代码对可空数组参数（align_series.columns/names、query_spot.symbols）断言顶层 `"type"=="array"`，与 spec §4.4 anyOf 渲染矛盾 | spec 权威；`tests/test_server.py` 加 `_prop()` 取非 null 分支，断言强度与 1.x 版一致（enum/required 仍直查）；另按 §4.3 发现并保真第二个无 description 参数 `query_arxiv.sort_by` |
| 5 | 计划 Task 3 `_handshake` 将 `notifications/initialized` 当请求等待 readline → 永久阻塞 | MCP 协议通知无应答（手动 echo 探针实证：仅 initialize 有一行应答）；新增 `notify()` 只写不等，其余断言逐字保留 |
| 6 | 计划 Task 5 Step 2 预期 README 零改动 → "扩展新数据源"节仍指示在已删除的 `build_tools()`/`handle_call_tool()` 注册路由 | 文档不得与现实相悖（仓库一贯原则）；同步为 `@mcp.tool` 薄函数 + `_summary_for` 分支两步，未涉版本表述 |

另有非偏差的审查 polish（并入 `0afed7c`）：server.py 注释措辞/import 归位/main docstring 措辞、契约回归文件头过时清单清理、`_prop` 失败信息增强。

## spec §6 验收标准逐条对照

| # | 验收标准 | 证据 | 结果 |
|---|---|---|---|
| 1 | 依赖 `mcp>=2.0.0,<3`，干净环境安装后可启动 | `pyproject.toml:7`（commit 2b26a48）；`.tmp-venv` 内 `pip install .` → `clean install ok`（exit 0），initialize 应答正常 | ✅ |
| 2 | initialize 探针应答含 `serverInfo.name` | `test_initialize_returns_server_info` PASSED；手动 echo 两次均返回 `"serverInfo":{"name":"local-datasource",...}` | ✅ |
| 3 | `tools/list` 列出 16 工具，名称与 `EXPECTED_TOOL_NAMES` 一致 | `test_tools_list_has_16` PASSED；`test_tools_count_and_names` 以集合相等断言 16 名单（含 EXPECTED_TOOL_NAMES 同一清单） | ✅ |
| 4 | 任选 3 工具 `tools/call` 真实调用，CSV 落盘+预览一致 | 探针 3 次成功调用：`query_trading_rules`(market=a) 与 `align_series` 端到端（2 工具 3 次调用），返回均含落盘路径+预览。**覆盖边界（有意取舍，计划 Task 5 Step 4 预授权记录）**：纯本地可离线稳定回归的工具只有 2 个；其余 14 个工具与 1.x 走完全相同 provider 层（既有测试覆盖） | ✅（含记录的覆盖边界） |
| 5 | `CoverageError` 样例：错误文本完整到达客户端，非裸 `Error executing tool` | stdio 真实路径 `test_business_error_text_reaches_client` PASSED（`Error calling align_series` + "输入文件不存在" + 无 "Error executing tool"）；进程内 `test_coverage_error_wrapped_in_process` PASSED（CoverageError 类型本身，"补数"指引完整） | ✅ |
| 6 | `download` CLI 分支回归可用 | `download --help` usage + exit=0；离线全量中 CLI 15 项测试全绿（含 no-subcommand 启动、缺 --config 用法错误） | ✅ |
| 7 | 契约回归测试 2.x 形状通过；providers 层测试全量通过 | 离线门控 `SKIP_INTEGRATION=1 pytest tests/ -q` → **226 passed, 32 skipped, 0 failed**（含全部 provider 测试与 2.x 形状契约测试）；无门控口径见文末 | ✅ |
| 8 | README/依赖表述无 1.x 残留 | `grep -in "1\.6\.0\|mcp.*<2\|mcp 1" README.md pyproject.toml CHANGELOG.md SKILL.md` → 零命中 | ✅ |

## 完成定义自检

1. spec §6 八条全部有证据：见上表。
2. 全量 pytest 绿、数量 = 基线 + 6：离线门控 226 = 220 + 6 ✅（无门控口径受偏差项 #0 网络环境影响，见下）。
3. 分支 `feature/mcp2-migration` 本地留存，未推送远端 ✅。
4. 本日志落盘，含逐任务记录、偏差项、最终 commit 哈希、§6 对照 ✅。

## 终版核验（日志提交前最后跑）

- 无门控全量 `python -m pytest tests/ -q`：**29 failed / 227 passed / 2 skipped**，1316s（21:56）。
  与基线（221 passed / 29 failed / 2 skipped）对比：**失败数与集合一致**（同为偏差项 #0 所述实网环境失败，迁移前即如此），通过数 221 + 6 = 227 恰为新增探针数——迁移零破坏，Task 0 Step 5"与基线相同"的无门控口径由此补验成立（除 Task 1 已修复的 2 个耦合文件外）。
- 离线门控全量：**226 passed / 32 skipped / 0 failed**。
- 版本残留 `grep -in "1\.6\.0\|mcp.*<2\|mcp 1" README.md pyproject.toml CHANGELOG.md SKILL.md` → 零命中（exit 1）。
- 分支 HEAD（迁移主体）：`8bdeefbe6f5fa0a9ce5c9143e615f9de16a664af`。
- 本日志与计划勾选随第 5 个提交入库（交付回传消息中的最终哈希以迁移主体 `8bdeefb` 为准）。

---

## 交付回传（cross-window send 备援记录）

> **发送失败说明**：按任务第 5 步尝试用 computer use 定位标题为「mcp2迁移」的 ZCode 窗口：`list_apps` 仅见一个 ZCode 进程（pid 17576）、`list_windows` 仅两个窗口且标题均为"ZCode"——其一为本执行会话窗口，另一为"ZCode 正在操作电脑"悬浮指示窗；两次切换侧边栏亦未在无障碍树中暴露名为「mcp2迁移」的会话/窗口。该目标窗口当前不可见（可能已被发送方关闭或位于其他虚拟桌面），消息无法投递，按指令回退至本日志备援。

**交付消息正文**（原定粘贴到「mcp2迁移」窗口的内容）：

MCP 2.x 迁移完成：`feature/mcp2-migration` 分支共 5 个提交，入口层已迁至 mcp 2.1.1 `MCPServer`（依赖 `mcp>=2.0.0,<3`），16 个工具的名称/required/enum/描述文本经独立脚本比对与基线逐字一致，providers/cache/CLI/formatters 零改动，未推送远端。测试结果：离线门控 `SKIP_INTEGRATION=1 pytest tests/ -q` → **226 passed / 32 skipped / 0 failed（全绿，= 迁移基线 220 + 新增 6 个 stdio 探针）**；无门控全量 227 passed / 29 failed（该 29 个失败为迁移前即存在的实网环境失败，与基线完全同集合，非本次改动引入）。偏差项 **7 个**（均不阻塞，逐项理由见本日志）。执行日志：`docs/superpowers/plans/2026-09-07-MCP2迁移-执行日志.md`。最终 commit：迁移主体 **`8bdeefb`**（本日志随 `5904c9d` 入库）。**请按 requesting-code-review 技能对本分支做代码评审。**
