# 债券与可转债数据源 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 local-datasource MCP server 新增「中国境内债券 + 可转债」数据源,基于 akshare,提供 2 个 MCP tool(`query_bond` / `query_convertible_bond`)共 7 个查询 kind。

**Architecture:** 沿用现有「一个数据域 = 一个 provider 文件 = 一个 tool」模式。新增 `providers/bond.py` 与 `providers/convertible_bond.py`,每个暴露 `query_xxx(...) -> (file_path, summary)` 函数,内部用 `kind` 字符串路由到不同 akshare 接口,最终经现有 `format_csv_output` 写 CSV。`server.py` 的 `build_tools()` 与 `handle_call_tool()` 各加 2 条。零新增依赖(akshare 项目已用),零 apikey,不动 config。

**Tech Stack:** Python 3.10+、akshare(已在 pyproject.toml)、mcp Python SDK、pytest。

---

## 设计依据

本计划严格对齐 `docs/superpowers/specs/2026-07-02-债券转债数据源-design.md`。三个实测固化的关键点(用真实代码 `2180495.IB` 验证):
1. `bond_info_cm(bond_code=…)` 做子串匹配,需精确过滤 `df[df['债券代码']==code]` 排除误命中(如 `2180495` 误命中 `112180495`)
2. `bond_info_detail_cm` 存在 akshare 上游 bug(列名 KeyError),本期不调用
3. 城投/非上市发行人(如徐州新盛)财务 akshare 免费层无,`issuer_finance` 解析正股失败时返回引导性提示而非报错

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/local_datasource/providers/bond.py` | 标债域 provider:`query_bond(kind, …)` 路由到 `yield_curve` / `issue_info` / `credit_daily` | 新建 |
| `src/local_datasource/providers/convertible_bond.py` | 转债域 provider:`query_convertible_bond(kind, …)` 路由到 `overview` / `terms` / `history` / `issuer_finance` | 新建 |
| `src/local_datasource/server.py` | 注册 2 个新 tool + 路由 | 修改 |
| `tests/test_bond.py` | 标债域纯函数 + 集成测试 | 新建 |
| `tests/test_convertible_bond.py` | 转债域纯函数 + 集成测试 | 新建 |
| `tests/test_server.py` | tool 数量断言 4→6 | 修改 |
| `README.md` | 覆盖范围表 + 调用示例 | 修改 |
| `SKILL.md` | 工具选择速查 + tool 规范 | 修改 |

---

## Task 1: 标债域 — `_normalize_bond_code` 归一化(TDD)

**Files:**
- Create: `src/local_datasource/providers/bond.py`
- Test: `tests/test_bond.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_bond.py`,内容:

```python
# tests/test_bond.py
import os
import tempfile

import pytest

from local_datasource.providers.bond import query_bond


def test_normalize_bond_code_strips_ib_suffix():
    """银行间代码 .IB 后缀需剥离,只保留数字。"""
    from local_datasource.providers import bond
    assert bond._normalize_bond_code("2180495.IB") == "2180495"


def test_normalize_bond_code_strips_exchange_suffix():
    """交易所代码 .SH/.SZ 后缀也需剥离。"""
    from local_datasource.providers import bond
    assert bond._normalize_bond_code("sh019623") == "sh019623"
    assert bond._normalize_bond_code("019623.SH") == "019623"


def test_normalize_bond_code_plain_digits():
    """纯数字原样返回。"""
    from local_datasource.providers import bond
    assert bond._normalize_bond_code("2180495") == "2180495"


def test_normalize_bond_code_invalid():
    """非数字+非交易所前缀格式应报错。"""
    from local_datasource.providers import bond
    with pytest.raises(ValueError):
        bond._normalize_bond_code("abc!@#")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_bond.py -v`
Expected: FAIL,`ModuleNotFoundError: No module named 'local_datasource.providers.bond'`

- [ ] **Step 3: 写最小实现**

创建 `src/local_datasource/providers/bond.py`,内容:

```python
"""中国境内债券(标债)数据 provider。

通过 akshare 获取国债收益率曲线、信用债发行信息与交易所行情,统一输出 CSV。

已知限制(akshare 免费层):
- 中债估值 YTM / 全价:无(Wind/中债登付费)
- 标债赎回回售条款详情 / 票息 / 到期日 / 剩余期限:无(在募集说明书里)
- bond_info_detail_cm 接口有上游 bug,本期不调用
"""
from __future__ import annotations

import re
from datetime import datetime
from typing import Literal

import akshare as ak
import pandas as pd

from local_datasource.formatters import format_csv_output


BondKind = Literal["yield_curve", "issue_info", "credit_daily"]


def _normalize_bond_code(code: str) -> str:
    """将债券代码归一为 akshare 所需格式。

    - 银行间代码 ``2180495.IB`` → ``2180495``(剥后缀,只留数字)
    - 交易所带前缀代码 ``sh019623`` 原样保留(akshare bond_zh_hs_daily 需要前缀)
    - 交易所带后缀代码 ``019623.SH`` → ``019623``
    - 非法格式(含非数字非前缀字符)报错
    """
    # 先剥 .IB / .SH / .SZ 后缀
    stripped = re.sub(r"\.(IB|SH|SZ|sh|sz|ib)$", "", code)
    # 纯数字 → 原样
    if stripped.isdigit():
        return stripped
    # sh/sz/bj 开头的交易所代码 → 原样
    if re.match(r"^(sh|sz|bj)\d+$", stripped, re.IGNORECASE):
        return stripped.lower()
    raise ValueError(f"Invalid bond code: {code}")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_bond.py -v`
Expected: 4 个 `test_normalize_bond_code_*` PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/bond.py tests/test_bond.py
git commit -m "feat(bond): add _normalize_bond_code normalizer with TDD"
```

---

## Task 2: 标债域 — `query_bond(yield_curve)` 国债收益率曲线(TDD)

**Files:**
- Modify: `src/local_datasource/providers/bond.py`
- Test: `tests/test_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_bond_yield_curve():
    """国债收益率曲线:返回各期限到期收益率,行数>0。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_bond(
            kind="yield_curve",
            start_date="2025-01-01",
            end_date="2025-06-30",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
        assert "Columns:" in summary
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_bond.py::test_query_bond_yield_curve -v`
Expected: FAIL,`TypeError: query_bond() missing`(函数尚未实现)

- [ ] **Step 3: 写最小实现**

在 `src/local_datasource/providers/bond.py` 的 `_normalize_bond_code` 之后追加:

```python
def _query_yield_curve(start_date: str, end_date: str) -> pd.DataFrame:
    """国债到期收益率曲线(bond_china_yield)。"""
    start_fmt = datetime.strptime(start_date, "%Y-%m-%d").strftime("%Y%m%d")
    end_fmt = datetime.strptime(end_date, "%Y-%m-%d").strftime("%Y%m%d")
    df = ak.bond_china_yield(start_date=start_fmt, end_date=end_fmt)
    if df.empty:
        raise ValueError(f"No yield curve data between {start_date} and {end_date}")
    return df


def query_bond(
    kind: BondKind,
    file_path: str,
    start_date: str | None = None,
    end_date: str | None = None,
    bond_code: str | None = None,
    symbol: str | None = None,
) -> tuple[str, str]:
    """查询中国境内债券数据并输出 CSV。

    参数:
        kind: ``yield_curve`` / ``issue_info`` / ``credit_daily``
        file_path: 输出 CSV 路径
        start_date: 开始日期 ``YYYY-MM-DD``(yield_curve / credit_daily 必填)
        end_date: 结束日期 ``YYYY-MM-DD``(yield_curve / credit_daily 必填)
        bond_code: 债券代码,如 ``2180495.IB`` / ``2180495``(issue_info 必填)
        symbol: 交易所债券代码,如 ``sh019623``(credit_daily 必填)
    """
    if kind == "yield_curve":
        if not start_date or not end_date:
            raise ValueError("yield_curve requires start_date and end_date")
        df = _query_yield_curve(start_date, end_date)
    elif kind == "issue_info":
        if not bond_code:
            raise ValueError("issue_info requires bond_code")
        df = _query_issue_info(bond_code)
    elif kind == "credit_daily":
        if not symbol or not start_date or not end_date:
            raise ValueError("credit_daily requires symbol, start_date, end_date")
        df = _query_credit_daily(symbol, start_date, end_date)
    else:
        raise ValueError(f"Unsupported bond kind: {kind}")

    if df.empty:
        raise ValueError(f"No data returned for bond kind={kind}")

    return format_csv_output(df, file_path)
```

注:`_query_issue_info` 与 `_query_credit_daily` 在 Task 3、Task 4 实现,本步先用占位会让 import 失败——因此本步**同时**加上这两个函数的最小桩,使其可 import 但 raise NotImplementedError:

在 `query_bond` 之前补充:

```python
def _query_issue_info(bond_code: str) -> pd.DataFrame:
    raise NotImplementedError("issue_info implemented in Task 3")


def _query_credit_daily(symbol: str, start_date: str, end_date: str) -> pd.DataFrame:
    raise NotImplementedError("credit_daily implemented in Task 4")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_bond.py::test_query_bond_yield_curve -v`
Expected: PASS(集成测试,跑真实 akshare,需联网)

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/bond.py tests/test_bond.py
git commit -m "feat(bond): add query_bond with yield_curve kind"
```

---

## Task 3: 标债域 — `query_bond(issue_info)` 发行信息 + 精确过滤(TDD)

**Files:**
- Modify: `src/local_datasource/providers/bond.py`
- Test: `tests/test_bond.py`

- [ ] **Step 1: 写失败测试(含真实回归 case)**

在 `tests/test_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_bond_issue_info_exact_match():
    """回归 case:2180495.IB 只应返回 1 条「21徐州新盛03」,排除子串误命中的 112180495。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_bond(
            kind="issue_info",
            bond_code="2180495.IB",
            file_path=path,
        )
        assert os.path.exists(file_path)
        # 读回 CSV 验证精确匹配
        import pandas as pd
        df = pd.read_csv(file_path)
        assert len(df) == 1, f"expected exactly 1 row, got {len(df)}:\n{df}"
        assert "徐州新盛03" in str(df.iloc[0].values) or "2180495" in str(df.iloc[0].values)
    finally:
        os.unlink(path)


def test_query_bond_issue_info_requires_bond_code():
    """issue_info 缺 bond_code 应报错(纯函数,无网络)。"""
    from local_datasource.providers import bond
    with pytest.raises(ValueError, match="issue_info requires bond_code"):
        bond.query_bond(kind="issue_info", file_path="/tmp/x.csv")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_bond.py::test_query_bond_issue_info_exact_match tests/test_bond.py::test_query_bond_issue_info_requires_bond_code -v`
Expected: 第一个 FAIL(`NotImplementedError`),第二个 FAIL(因为桩函数 raise NotImplementedError 而非 ValueError)

- [ ] **Step 3: 写实现(替换 Task 2 的桩)**

将 `src/local_datasource/providers/bond.py` 中的 `_query_issue_info` 桩替换为:

```python
def _query_issue_info(bond_code: str) -> pd.DataFrame:
    """信用债发行信息(bond_info_cm),精确过滤排除子串误匹配。

    实测 bond_info_cm(bond_code='2180495') 会返回 2 条:
      - 21徐州新盛03 / 2180495(目标)
      - 21赣州银行CD111 / 112180495(子串误命中)
    必须精确匹配 df['债券代码']==code 后再返回。
    """
    code = _normalize_bond_code(bond_code)
    df = ak.bond_info_cm(bond_code=code)
    if df.empty:
        raise ValueError(f"No bond found for code: {bond_code}")
    # 精确过滤,排除子串误命中
    df = df[df["债券代码"].astype(str) == code].copy()
    if df.empty:
        raise ValueError(f"No exact match for bond code: {bond_code}")
    return df
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_bond.py::test_query_bond_issue_info_exact_match tests/test_bond.py::test_query_bond_issue_info_requires_bond_code -v`
Expected: 两个都 PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/bond.py tests/test_bond.py
git commit -m "feat(bond): add issue_info kind with exact-match filter for 2180495.IB regression"
```

---

## Task 4: 标债域 — `query_bond(credit_daily)` 交易所日行情(TDD)

**Files:**
- Modify: `src/local_datasource/providers/bond.py`
- Test: `tests/test_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_bond_credit_daily():
    """信用债交易所日行情:返回开高低收量。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_bond(
            kind="credit_daily",
            symbol="sh019623",
            start_date="2025-01-01",
            end_date="2025-06-30",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_bond.py::test_query_bond_credit_daily -v`
Expected: FAIL(`NotImplementedError`)

- [ ] **Step 3: 写实现(替换 Task 2 的桩)**

将 `src/local_datasource/providers/bond.py` 中的 `_query_credit_daily` 桩替换为:

```python
def _query_credit_daily(symbol: str, start_date: str, end_date: str) -> pd.DataFrame:
    """信用债交易所历史日行情(bond_zh_hs_daily),按日期过滤。"""
    # bond_zh_hs_daily 需要带交易所前缀的 symbol(sh/sz)
    if not re.match(r"^(sh|sz)\d+$", symbol, re.IGNORECASE):
        raise ValueError(f"credit_daily symbol must be like sh019623, got: {symbol}")
    df = ak.bond_zh_hs_daily(symbol=symbol.lower())
    if df.empty:
        raise ValueError(f"No data returned for {symbol}")
    # 统一 date 列为 YYYY-MM-DD 字符串后过滤(复用 stock.py 的模式)
    df = df.copy()
    df["date"] = pd.to_datetime(df["date"]).dt.strftime("%Y-%m-%d")
    return df[(df["date"] >= start_date) & (df["date"] <= end_date)].copy()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_bond.py::test_query_bond_credit_daily -v`
Expected: PASS(集成,需联网)

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/bond.py tests/test_bond.py
git commit -m "feat(bond): add credit_daily kind for exchange bond history"
```

---

## Task 5: 标债域 — 跑全部 bond 测试 + 清理桩注释

**Files:**
- Verify: `src/local_datasource/providers/bond.py`

- [ ] **Step 1: 跑全部 bond 测试**

Run: `python -m pytest tests/test_bond.py -v`
Expected: 全部 PASS(4 个 normalize 纯函数 + 3 个集成,共 7 个)。若集成测试因网络失败,设 `SKIP_INTEGRATION=1` 跑纯函数:`SKIP_INTEGRATION=1 python -m pytest tests/test_bond.py -v`,应 4 PASS。

- [ ] **Step 2: 提交(如有改动)**

```bash
git add -A
git commit -m "chore(bond): verify all bond tests pass" --allow-empty
```

---

## Task 6: 转债域 — `_normalize_cb_code` 归一化(TDD)

**Files:**
- Create: `src/local_datasource/providers/convertible_bond.py`
- Test: `tests/test_convertible_bond.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_convertible_bond.py`,内容:

```python
# tests/test_convertible_bond.py
import os
import tempfile

import pytest

from local_datasource.providers.convertible_bond import query_convertible_bond


def test_normalize_cb_code_native():
    """akshare 原生格式 sz128039 原样返回(小写)。"""
    from local_datasource.providers import convertible_bond as cb
    assert cb._normalize_cb_code("sz128039") == "sz128039"


def test_normalize_cb_code_plain_digits_sz():
    """纯数字 128039 → sz128039(深市转债)。"""
    from local_datasource.providers import convertible_bond as cb
    assert cb._normalize_cb_code("128039") == "sz128039"


def test_normalize_cb_code_plain_digits_sh():
    """纯数字 113682 → sh113682(沪市转债)。"""
    from local_datasource.providers import convertible_bond as cb
    assert cb._normalize_cb_code("113682") == "sh113682"


def test_normalize_cb_code_suffix():
    """带后缀 113682.SH → sh113682。"""
    from local_datasource.providers import convertible_bond as cb
    assert cb._normalize_cb_code("113682.SH") == "sh113682"


def test_normalize_cb_code_invalid():
    """非法格式报错。"""
    from local_datasource.providers import convertible_bond as cb
    with pytest.raises(ValueError):
        cb._normalize_cb_code("abc!@#")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_convertible_bond.py -v`
Expected: FAIL,`ModuleNotFoundError: No module named 'local_datasource.providers.convertible_bond'`

- [ ] **Step 3: 写最小实现**

创建 `src/local_datasource/providers/convertible_bond.py`,内容:

```python
"""可转债数据 provider。

通过 akshare 获取可转债一览、条款、历史K线与发行人财务,统一输出 CSV。
"""
from __future__ import annotations

import re
from typing import Literal

import akshare as ak
import pandas as pd

from local_datasource.formatters import format_csv_output


CbKind = Literal["overview", "terms", "history", "issuer_finance"]
ReportType = Literal["资产负债表", "利润表", "现金流量表"]


def _normalize_cb_code(code: str) -> str:
    """将可转债代码归一为 akshare 所需的 ``shxxxxxx`` / ``szxxxxxx`` 格式。

    转债代码首位决定市场:
    - 11 开头(如 113682)→ 沪市 sh
    - 12 开头(如 128039)→ 深市 sz
    """
    stripped = re.sub(r"\.(SH|SZ|sh|sz)$", "", code)
    # 已带前缀
    if re.match(r"^(sh|sz)\d+$", stripped, re.IGNORECASE):
        return stripped.lower()
    # 纯数字 → 按首位补前缀
    if stripped.isdigit() and len(stripped) == 6:
        if stripped.startswith("11"):
            return f"sh{stripped}"
        if stripped.startswith("12"):
            return f"sz{stripped}"
        raise ValueError(f"Unrecognized CB code prefix: {code}")
    raise ValueError(f"Invalid CB code: {code}")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_convertible_bond.py -v`
Expected: 5 个 `test_normalize_cb_code_*` PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/convertible_bond.py tests/test_convertible_bond.py
git commit -m "feat(cb): add _normalize_cb_code normalizer with TDD"
```

---

## Task 7: 转债域 — `query_convertible_bond(overview)` 一览(TDD)

**Files:**
- Modify: `src/local_datasource/providers/convertible_bond.py`
- Test: `tests/test_convertible_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_convertible_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_cb_overview():
    """可转债一览:返回全市场转债,含转股溢价率等字段,行数>0。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_convertible_bond(
            kind="overview",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_overview -v`
Expected: FAIL(`query_convertible_bond` 未定义)

- [ ] **Step 3: 写最小实现(含其他 kind 的桩)**

在 `src/local_datasource/providers/convertible_bond.py` 的 `_normalize_cb_code` 之后追加:

```python
def _query_overview(keyword: str | None = None) -> pd.DataFrame:
    """可转债一览(bond_zh_cov),含转股溢价率/评级/规模。可选关键字过滤。"""
    df = ak.bond_zh_cov()
    if df.empty:
        raise ValueError("No convertible bond overview data")
    if keyword:
        # 在债券简称或正股简称里做关键字过滤
        mask = df["债券简称"].astype(str).str.contains(keyword, na=False) | \
               df["正股简称"].astype(str).str.contains(keyword, na=False)
        df = df[mask].copy()
        if df.empty:
            raise ValueError(f"No CB matched keyword: {keyword}")
    return df


def _query_terms() -> pd.DataFrame:
    raise NotImplementedError("terms implemented in Task 8")


def _query_history(symbol: str, start_date: str, end_date: str, period: str) -> pd.DataFrame:
    raise NotImplementedError("history implemented in Task 9")


def _query_issuer_finance(bond_code: str | None, stock_code: str | None, report_type: str) -> pd.DataFrame:
    raise NotImplementedError("issuer_finance implemented in Task 10")


def query_convertible_bond(
    kind: CbKind,
    file_path: str,
    symbol: str | None = None,
    start_date: str | None = None,
    end_date: str | None = None,
    period: str = "daily",
    bond_code: str | None = None,
    stock_code: str | None = None,
    report_type: ReportType = "资产负债表",
    keyword: str | None = None,
) -> tuple[str, str]:
    """查询可转债数据并输出 CSV。

    参数:
        kind: ``overview`` / ``terms`` / ``history`` / ``issuer_finance``
        file_path: 输出 CSV 路径
        symbol: 转债代码,如 ``sz128039``(history 必填)
        start_date / end_date: 日期 ``YYYY-MM-DD``(history 必填)
        period: ``daily`` / ``min``(history,默认 daily)
        bond_code: 转债代码(issuer_finance,与 stock_code 二选一)
        stock_code: 正股代码(issuer_finance,与 bond_code 二选一)
        report_type: ``资产负债表`` / ``利润表`` / ``现金流量表``(issuer_finance,默认资产负债表)
        keyword: 关键字过滤(overview 可选)
    """
    if kind == "overview":
        df = _query_overview(keyword=keyword)
    elif kind == "terms":
        df = _query_terms()
    elif kind == "history":
        if not symbol or not start_date or not end_date:
            raise ValueError("history requires symbol, start_date, end_date")
        df = _query_history(symbol, start_date, end_date, period)
    elif kind == "issuer_finance":
        if not bond_code and not stock_code:
            raise ValueError("issuer_finance requires bond_code or stock_code")
        df = _query_issuer_finance(bond_code, stock_code, report_type)
    else:
        raise ValueError(f"Unsupported CB kind: {kind}")

    if df.empty:
        raise ValueError(f"No data returned for CB kind={kind}")

    return format_csv_output(df, file_path)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_overview -v`
Expected: PASS(集成,需联网)

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/convertible_bond.py tests/test_convertible_bond.py
git commit -m "feat(cb): add query_convertible_bond with overview kind"
```

---

## Task 8: 转债域 — `query_convertible_bond(terms)` 条款+剩余期限(TDD)

**Files:**
- Modify: `src/local_datasource/providers/convertible_bond.py`
- Test: `tests/test_convertible_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_convertible_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_cb_terms():
    """可转债条款:集思录强赎+剩余期限/到期税前收益,行数>0。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_convertible_bond(
            kind="terms",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
        assert "Rows: 0" not in summary
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_terms -v`
Expected: FAIL(`NotImplementedError`)

- [ ] **Step 3: 写实现(替换 Task 7 的桩)**

将 `_query_terms` 桩替换为:

```python
def _query_terms() -> pd.DataFrame:
    """可转债条款信息:集思录强赎状态 + 剩余期限/到期税前收益。

    合并 bond_cb_redeem_jsl()(强赎/回售/下修)与 bond_cb_summary()(剩余期限/到期收益)。
    若 bond_cb_summary 不可用则仅返回强赎表。
    """
    redeem = ak.bond_cb_redeem_jsl()
    if redeem is None or redeem.empty:
        raise ValueError("No CB redeem data from jsl")
    try:
        summary = ak.bond_cb_summary()
        if summary is not None and not summary.empty:
            # 按债券代码合并两张表
            return redeem.merge(summary, on="债券代码", how="left")
    except Exception:
        # bond_cb_summary 不可用时降级为仅返回强赎表
        pass
    return redeem
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_terms -v`
Expected: PASS(集成,需联网)。若集思录接口当前不可用,测试可能失败——属真实环境波动,记录后继续。

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/convertible_bond.py tests/test_convertible_bond.py
git commit -m "feat(cb): add terms kind merging jsl redeem + summary"
```

---

## Task 9: 转债域 — `query_convertible_bond(history)` 历史K线(TDD)

**Files:**
- Modify: `src/local_datasource/providers/convertible_bond.py`
- Test: `tests/test_convertible_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_convertible_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_cb_history_daily():
    """可转债历史日K:sz128039 返回开高低收量。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_convertible_bond(
            kind="history",
            symbol="sz128039",
            start_date="2024-01-01",
            end_date="2024-06-30",
            period="daily",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
        assert "Rows: 0" not in summary
    finally:
        os.unlink(path)


def test_query_cb_history_requires_symbol():
    """history 缺 symbol 应报错。"""
    from local_datasource.providers import convertible_bond as cb
    with pytest.raises(ValueError, match="history requires"):
        cb.query_convertible_bond(kind="history", start_date="2024-01-01", end_date="2024-06-30", file_path="/tmp/x.csv")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_history_daily tests/test_convertible_bond.py::test_query_cb_history_requires_symbol -v`
Expected: 第一个 FAIL(`NotImplementedError`),第二个 FAIL(桩 raise NotImplementedError 而非 ValueError)

- [ ] **Step 3: 写实现(替换 Task 7 的桩)**

将 `_query_history` 桩替换为:

```python
def _query_history(symbol: str, start_date: str, end_date: str, period: str) -> pd.DataFrame:
    """可转债历史K线。

    period=daily → bond_zh_hs_cov_daily(日K)
    period=min  → bond_zh_hs_cov_min(分钟K)
    """
    code = _normalize_cb_code(symbol)
    if period == "daily":
        df = ak.bond_zh_hs_cov_daily(symbol=code)
        if df.empty:
            raise ValueError(f"No daily history for {symbol}")
        # bond_zh_hs_cov_daily 返回 date 列,统一格式后过滤
        df = df.copy()
        df["date"] = pd.to_datetime(df["date"]).dt.strftime("%Y-%m-%d")
        return df[(df["date"] >= start_date) & (df["date"] <= end_date)].copy()
    elif period == "min":
        df = ak.bond_zh_hs_cov_min(symbol=code)
        if df.empty:
            raise ValueError(f"No minute history for {symbol}")
        return df
    else:
        raise ValueError(f"Unsupported period: {period}, use 'daily' or 'min'")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_history_daily tests/test_convertible_bond.py::test_query_cb_history_requires_symbol -v`
Expected: 两个 PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/convertible_bond.py tests/test_convertible_bond.py
git commit -m "feat(cb): add history kind with daily/min K-line"
```

---

## Task 10: 转债域 — `query_convertible_bond(issuer_finance)` 发行人财务(TDD)

**Files:**
- Modify: `src/local_datasource/providers/convertible_bond.py`
- Test: `tests/test_convertible_bond.py`

- [ ] **Step 1: 写失败测试(含城投非上市回归 case)**

在 `tests/test_convertible_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_cb_issuer_finance_via_stock_code():
    """通过正股代码取发行人财务:603938(益丰药房)返回三大报表。"""
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_convertible_bond(
            kind="issuer_finance",
            stock_code="sh603938",
            report_type="资产负债表",
            file_path=path,
        )
        assert os.path.exists(file_path)
        assert "Rows:" in summary
    finally:
        os.unlink(path)


def test_query_cb_issuer_finance_requires_code():
    """issuer_finance 缺 bond_code 且 stock_code 应报错。"""
    from local_datasource.providers import convertible_bond as cb
    with pytest.raises(ValueError, match="issuer_finance requires"):
        cb.query_convertible_bond(kind="issuer_finance", file_path="/tmp/x.csv")


def test_query_cb_issuer_finance_nonlisted_returns_hint():
    """非上市发行人(城投)解析正股失败时返回引导性提示 CSV,不报错。

    纯函数测试:mock _resolve_stock_code_from_cb 返回 None,
    验证返回的 DataFrame 含引导性文案而非抛异常。
    """
    from local_datasource.providers import convertible_bond as cb
    import pandas as pd
    # 用 bond_code 触发解析;非上市发行人会解析失败
    # 这里直接测 _build_nonlisted_hint 输出格式
    hint_df = cb._build_nonlisted_hint("某城投平台")
    assert len(hint_df) == 1
    assert "非上市" in str(hint_df.iloc[0].values) or "Wind" in str(hint_df.iloc[0].values)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_issuer_finance_via_stock_code tests/test_convertible_bond.py::test_query_cb_issuer_finance_requires_code tests/test_convertible_bond.py::test_query_cb_issuer_finance_nonlisted_returns_hint -v`
Expected: 前两个 FAIL(`NotImplementedError`),第三个 FAIL(`AttributeError: _build_nonlisted_hint` 不存在)

- [ ] **Step 3: 写实现(替换 Task 7 的桩)**

将 `_query_issuer_finance` 桩替换为下面这一组函数(放在 `query_convertible_bond` 之前):

```python
def _resolve_stock_code_from_cb(bond_code: str) -> tuple[str, str] | None:
    """从可转债代码解析出正股代码与发行人名称。

    通过 bond_zh_cov() 全表过滤出对应正股代码。
    返回 (正股代码, 发行人名称) 或 None(非上市发行人/未找到)。
    """
    code = _normalize_cb_code(bond_code)
    # bond_zh_cov 的「债券代码」是纯数字(如 113682),需剥前缀
    plain_code = re.sub(r"^(sh|sz)", "", code)
    df = ak.bond_zh_cov()
    if df.empty:
        return None
    row = df[df["债券代码"].astype(str) == plain_code]
    if row.empty:
        return None
    stock_code = str(row.iloc[0]["正股代码"])
    issuer_name = str(row.iloc[0]["正股简称"])
    # 补交易所前缀
    if stock_code.startswith("6"):
        stock_code = f"sh{stock_code}"
    else:
        stock_code = f"sz{stock_code}"
    return stock_code, issuer_name


def _build_nonlisted_hint(issuer_name: str) -> pd.DataFrame:
    """构造非上市发行人财务缺口的引导性提示 DataFrame(不报错)。"""
    return pd.DataFrame([{
        "提示": f"发行人 {issuer_name} 为非上市主体,免费层无财务数据,建议查 Wind/企业预警通",
    }])


def _query_issuer_finance(
    bond_code: str | None,
    stock_code: str | None,
    report_type: str,
) -> pd.DataFrame:
    """发行人财务三大报表。

    - stock_code 给定 → 直接查该正股
    - bond_code 给定 → 先解析正股代码,再查;解析失败(城投/非上市)→ 返回引导性提示
    """
    if stock_code:
        # 直接用正股代码(补前缀)
        sc = stock_code.lower()
        if not re.match(r"^(sh|sz)\d+$", sc):
            if sc.isdigit() and len(sc) == 6:
                sc = f"sh{sc}" if sc.startswith("6") else f"sz{sc}"
            else:
                raise ValueError(f"Invalid stock_code: {stock_code}")
        df = ak.stock_financial_report_sina(stock=sc, symbol=report_type)
        if df.empty:
            raise ValueError(f"No financial data for stock {stock_code}")
        return df
    # bond_code → 解析正股
    resolved = _resolve_stock_code_from_cb(bond_code)
    if resolved is None:
        # 城投/非上市发行人:返回引导性提示而非报错
        return _build_nonlisted_hint(bond_code or "未知发行人")
    stock_code_resolved, issuer_name = resolved
    df = ak.stock_financial_report_sina(stock=stock_code_resolved, symbol=report_type)
    if df.empty:
        return _build_nonlisted_hint(issuer_name)
    return df
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_convertible_bond.py::test_query_cb_issuer_finance_via_stock_code tests/test_convertible_bond.py::test_query_cb_issuer_finance_requires_code tests/test_convertible_bond.py::test_query_cb_issuer_finance_nonlisted_returns_hint -v`
Expected: 三个 PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/convertible_bond.py tests/test_convertible_bond.py
git commit -m "feat(cb): add issuer_finance with stock resolution + non-listed hint"
```

---

## Task 11: 转债域 — 跑全部 CB 测试

**Files:**
- Verify: `src/local_datasource/providers/convertible_bond.py`

- [ ] **Step 1: 跑全部 CB 测试**

Run: `python -m pytest tests/test_convertible_bond.py -v`
Expected: 全部 PASS(5 个 normalize 纯函数 + 4 个集成 + 2 个纯函数 requires/hint,共 11 个)。集成失败时 `SKIP_INTEGRATION=1 python -m pytest tests/test_convertible_bond.py -v`,应 7 PASS。

- [ ] **Step 2: 提交(如有改动)**

```bash
git add -A
git commit -m "chore(cb): verify all CB tests pass" --allow-empty
```

---

## Task 12: server.py — 注册 2 个新 tool + 路由(TDD)

**Files:**
- Modify: `src/local_datasource/server.py:24-27`(import)、`server.py:33-97`(build_tools)、`server.py:100-117`(handle_call_tool)
- Modify: `tests/test_server.py`

- [ ] **Step 1: 写失败测试**

将 `tests/test_server.py` 替换为:

```python
# tests/test_server.py
from local_datasource.server import build_tools


def test_build_tools_count():
    tools = build_tools()
    assert len(tools) == 6
    names = {t.name for t in tools}
    assert names == {
        "query_stock",
        "query_yfinance",
        "query_worldbank",
        "query_arxiv",
        "query_bond",
        "query_convertible_bond",
    }


def test_query_bond_schema_has_kind_enum():
    """query_bond 的 inputSchema 必须含 kind 枚举。"""
    tools = {t.name: t for t in build_tools()}
    schema = tools["query_bond"].inputSchema
    assert "kind" in schema["properties"]
    assert set(schema["properties"]["kind"]["enum"]) == {"yield_curve", "issue_info", "credit_daily"}


def test_query_convertible_bond_schema_has_kind_enum():
    """query_convertible_bond 的 inputSchema 必须含 kind 枚举。"""
    tools = {t.name: t for t in build_tools()}
    schema = tools["query_convertible_bond"].inputSchema
    assert "kind" in schema["properties"]
    assert set(schema["properties"]["kind"]["enum"]) == {"overview", "terms", "history", "issuer_finance"}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_server.py -v`
Expected: FAIL(`len(tools) == 4` 断言失败,实际仍为 4)

- [ ] **Step 3: 修改 server.py — 加 import**

在 `src/local_datasource/server.py` 第 24-27 行的 import 块,把:

```python
from local_datasource.config import load_config
from local_datasource.providers.arxiv import query_arxiv
from local_datasource.providers.stock import query_stock
from local_datasource.providers.worldbank import query_worldbank
from local_datasource.providers.yahoo import query_yfinance
```

改为(按字母序插入 bond 与 convertible_bond):

```python
from local_datasource.config import load_config
from local_datasource.providers.arxiv import query_arxiv
from local_datasource.providers.bond import query_bond
from local_datasource.providers.convertible_bond import query_convertible_bond
from local_datasource.providers.stock import query_stock
from local_datasource.providers.worldbank import query_worldbank
from local_datasource.providers.yahoo import query_yfinance
```

同时更新文件顶部 docstring,把「注册 4 个数据查询 tool」改为「注册 6 个数据查询 tool」,并在列表中补上:

```
- ``query_bond``：中国境内债券（国债收益率曲线/信用债发行信息/交易所行情）
- ``query_convertible_bond``：可转债（一览/条款/历史K线/发行人财务）
```

- [ ] **Step 4: 修改 server.py — build_tools 加 2 个 Tool**

在 `build_tools()` 的 `return [` 列表末尾(`query_arxiv` Tool 之后、`]` 之前)追加:

```python
        Tool(
            name="query_bond",
            description=(
                "Query China onshore bonds. Output is written to file_path as CSV. "
                "kind=yield_curve: 国债到期收益率曲线 (bond_china_yield). "
                "kind=issue_info: 信用债发行信息含评级 (bond_info_cm). "
                "kind=credit_daily: 信用债交易所日行情 (bond_zh_hs_daily). "
                "已知限制(akshare免费层无): 中债估值YTM/全价、赎回回售条款详情、剩余期限、城投发行人财务。"
            ),
            inputSchema={
                "type": "object",
                "properties": {
                    "kind": {"type": "string", "enum": ["yield_curve", "issue_info", "credit_daily"], "description": "Query type"},
                    "file_path": {"type": "string", "description": "Output CSV file path"},
                    "start_date": {"type": "string", "description": "Start date YYYY-MM-DD (yield_curve/credit_daily)"},
                    "end_date": {"type": "string", "description": "End date YYYY-MM-DD (yield_curve/credit_daily)"},
                    "bond_code": {"type": "string", "description": "Bond code e.g. 2180495.IB or 2180495 (issue_info)"},
                    "symbol": {"type": "string", "description": "Exchange bond symbol e.g. sh019623 (credit_daily)"},
                },
                "required": ["kind", "file_path"],
            },
        ),
        Tool(
            name="query_convertible_bond",
            description=(
                "Query China convertible bonds. Output is written to file_path as CSV. "
                "kind=overview: 全市场一览含转股溢价率/评级/规模 (bond_zh_cov). "
                "kind=terms: 强赎/回售/下修条款+剩余期限 (集思录). "
                "kind=history: 单只转债历史K线 daily/min (bond_zh_hs_cov_daily/min). "
                "kind=issuer_finance: 发行人正股三大报表;城投/非上市发行人返回引导性提示。"
            ),
            inputSchema={
                "type": "object",
                "properties": {
                    "kind": {"type": "string", "enum": ["overview", "terms", "history", "issuer_finance"], "description": "Query type"},
                    "file_path": {"type": "string", "description": "Output CSV file path"},
                    "symbol": {"type": "string", "description": "CB symbol e.g. sz128039 (history)"},
                    "start_date": {"type": "string", "description": "Start date YYYY-MM-DD (history)"},
                    "end_date": {"type": "string", "description": "End date YYYY-MM-DD (history)"},
                    "period": {"type": "string", "enum": ["daily", "min"], "default": "daily", "description": "K-line period (history)"},
                    "bond_code": {"type": "string", "description": "CB code (issuer_finance, mutually exclusive with stock_code)"},
                    "stock_code": {"type": "string", "description": "Underlying stock code (issuer_finance, mutually exclusive with bond_code)"},
                    "report_type": {"type": "string", "enum": ["资产负债表", "利润表", "现金流量表"], "default": "资产负债表", "description": "Financial report type (issuer_finance)"},
                    "keyword": {"type": "string", "description": "Keyword filter (overview, optional)"},
                },
                "required": ["kind", "file_path"],
            },
        ),
```

- [ ] **Step 5: 修改 server.py — handle_call_tool 加路由**

在 `handle_call_tool()` 的 `elif` 链中(`elif name == "query_arxiv":` 之后、`else:` 之前)插入:

```python
        elif name == "query_bond":
            _, summary = query_bond(**arguments)
        elif name == "query_convertible_bond":
            _, summary = query_convertible_bond(**arguments)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `python -m pytest tests/test_server.py -v`
Expected: 3 个 PASS

- [ ] **Step 7: 提交**

```bash
git add src/local_datasource/server.py tests/test_server.py
git commit -m "feat(server): register query_bond and query_convertible_bond tools"
```

---

## Task 13: 全量回归测试

**Files:**
- Verify: 全项目测试

- [ ] **Step 1: 跑全量测试**

Run: `python -m pytest tests/ -v`
Expected: 全部 PASS(原有测试不受影响 + 新增 bond/cb 测试)。集成测试受网络波动影响可能偶发失败,设 `SKIP_INTEGRATION=1 python -m pytest tests/ -v` 跑纯函数子集应全 PASS。

- [ ] **Step 2: 确认无回归**

确认 `test_stock.py`、`test_worldbank.py`、`test_arxiv.py`、`test_yahoo.py`、`test_config.py`、`test_formatters.py` 仍全 PASS。

- [ ] **Step 3: 提交(如有改动,否则空提交标记)**

```bash
git add -A
git commit -m "test: full regression after bond/cb integration" --allow-empty
```

---

## Task 14: 更新 README.md

**Files:**
- Modify: `README.md:72-80`(覆盖范围表)、README 调用示例章节

- [ ] **Step 1: 更新覆盖范围表**

在 `README.md` 第 72-79 行的覆盖范围表中,在 `arXiv 学术论文` 行之后追加 2 行:

```markdown
| 中国境内债券（国债收益率/信用债） | `query_bond` | `akshare` | 否 |
| 可转债（一览/条款/历史/发行人财务） | `query_convertible_bond` | `akshare` | 否 |
```

- [ ] **Step 2: 更新架构图(可选,若架构章节列了底层接口)**

确认 `README.md` 第 85-93 行架构图中的「akshare / yfinance / wbgapi / arxiv」行无需改动(akshare 已在用)。无需修改则跳过此步。

- [ ] **Step 3: 在调用示例章节追加 2 个 JSON 示例**

在 README 的「调用数据案例」章节(约第 213-288 行)末尾追加:

```markdown
### query_bond — 国债收益率曲线

```json
{
  "tool": "query_bond",
  "arguments": {
    "kind": "yield_curve",
    "start_date": "2025-01-01",
    "end_date": "2025-06-30",
    "file_path": "output/bond_yield.csv"
  }
}
```

### query_bond — 信用债发行信息

```json
{
  "tool": "query_bond",
  "arguments": {
    "kind": "issue_info",
    "bond_code": "2180495.IB",
    "file_path": "output/bond_issue.csv"
  }
}
```

### query_convertible_bond — 可转债一览

```json
{
  "tool": "query_convertible_bond",
  "arguments": {
    "kind": "overview",
    "file_path": "output/cb_overview.csv"
  }
}
```

### query_convertible_bond — 发行人财务

```json
{
  "tool": "query_convertible_bond",
  "arguments": {
    "kind": "issuer_finance",
    "stock_code": "sh603938",
    "report_type": "资产负债表",
    "file_path": "output/issuer_finance.csv"
  }
}
```
```

- [ ] **Step 4: 提交**

```bash
git add README.md
git commit -m "docs: add bond & CB tools to README coverage table and examples"
```

---

## Task 15: 更新 SKILL.md

**Files:**
- Modify: `SKILL.md:15`(tool 数量)、`SKILL.md:35-40`(工具选择速查表)、新增 tool 规范章节

- [ ] **Step 1: 更新 tool 数量描述**

将 `SKILL.md` 第 15 行「本 skill 通过本地 MCP server 提供 4 个数据查询工具」改为「本 skill 通过本地 MCP server 提供 6 个数据查询工具」。

- [ ] **Step 2: 工具选择速查表追加 2 行**

在 `SKILL.md` 第 37-40 行的工具选择速查表中追加:

```markdown
| 国债收益率曲线 / 信用债发行信息 / 交易所行情 | `query_bond` | kind 分流,无需 key |
| 可转债一览 / 条款 / 历史K线 / 发行人财务 | `query_convertible_bond` | kind 分流,发行人财务支持正股代码直查 |
```

- [ ] **Step 3: 新增 2 个 tool 的规范章节**

在 SKILL.md 末尾(或现有 tool 规范章节之后)追加:

```markdown
## query_bond — 中国境内债券

### 必填参数
- `kind`: `yield_curve` / `issue_info` / `credit_daily`
- `file_path`: 输出 CSV 路径

### 可选参数
- `start_date` / `end_date`(`YYYY-MM-DD`,yield_curve / credit_daily 必填)
- `bond_code`(issue_info 必填,支持 `2180495.IB` / `2180495` 格式)
- `symbol`(credit_daily 必填,如 `sh019623`)

### Agent 注意事项
- `issue_info` 已做精确过滤,`2180495.IB` 只返回 1 条对应债券,不会误命中同号段存单。
- 国债收益率曲线返回各期限(1/3/5/7/10年)到期收益率。

### 已知限制
akshare 免费层无法获取:中债估值 YTM / 全价、赎回回售条款详情、剩余期限、城投/非上市发行人财务。需要这些数据时建议提示用户查 Wind / 企业预警通。

## query_convertible_bond — 可转债

### 必填参数
- `kind`: `overview` / `terms` / `history` / `issuer_finance`
- `file_path`: 输出 CSV 路径

### 可选参数
- `symbol`(history 必填,如 `sz128039`)
- `start_date` / `end_date`(history 必填)
- `period`:`daily` / `min`(history,默认 daily)
- `bond_code` / `stock_code`(issuer_finance 二选一)
- `report_type`:`资产负债表` / `利润表` / `现金流量表`(issuer_finance,默认资产负债表)
- `keyword`(overview 可选,按债券简称/正股简称过滤)

### Agent 注意事项
- `issuer_finance` 用 `bond_code` 时自动解析正股;若发行人为城投/非上市主体(如银行间信用债),返回引导性提示而非报错。
- `terms` 合并集思录强赎与剩余期限数据。

### 已知限制
- 集思录接口(`bond_cb_redeem_jsl` / `bond_cb_summary`)偶发不可用,失败时 terms 仅返回强赎表。
```

- [ ] **Step 4: 提交**

```bash
git add SKILL.md
git commit -m "docs: add query_bond & query_convertible_bond to SKILL.md"
```

---

## Task 16: 端到端冒烟验证

**Files:**
- Verify: 通过 MCP tool 调用链路

- [ ] **Step 1: 验证 server 可启动**

Run: `python -c "from local_datasource.server import build_tools; print(len(build_tools()))"`
Expected: 输出 `6`

- [ ] **Step 2: 验证 provider 可直接调用(不走 MCP)**

Run:
```bash
python -c "
from local_datasource.providers.bond import query_bond
from local_datasource.providers.convertible_bond import query_convertible_bond
# 标债发行信息(真实回归 case)
p, s = query_bond(kind='issue_info', bond_code='2180495.IB', file_path='/tmp/smoke_bond.csv')
print('BOND issue_info:', s.split(chr(10))[1])
# 转债一览
p, s = query_convertible_bond(kind='overview', file_path='/tmp/smoke_cb.csv')
print('CB overview:', s.split(chr(10))[1])
"
```
Expected: 打印 `BOND issue_info: Rows: 1, ...` 与 `CB overview: Rows: <N>, ...`(N 为当前在市转债数,通常 >500)

- [ ] **Step 3: 提交(空提交标记冒烟通过)**

```bash
git add -A
git commit -m "chore: smoke test passes for bond & CB tools" --allow-empty
```

---

## Self-Review

**1. Spec coverage 逐项核对:**

| Spec 要求 | 对应 Task |
|---|---|
| `query_bond` yield_curve | Task 2 |
| `query_bond` issue_info + 精确过滤 | Task 3 |
| `query_bond` credit_daily | Task 4 |
| `query_convertible_bond` overview | Task 7 |
| `query_convertible_bond` terms | Task 8 |
| `query_convertible_bond` history(daily+min) | Task 9 |
| `query_convertible_bond` issuer_finance(bond_code/stock_code 二选一) | Task 10 |
| ticker 归一化 `_normalize_bond_code` | Task 1 |
| ticker 归一化 `_normalize_cb_code` | Task 6 |
| 放弃 `bond_info_detail_cm` | Task 3 实现中不调用 ✓ |
| 城投非上市返回引导性提示 | Task 10 `_build_nonlisted_hint` |
| 日期格式校验 | Task 2/4/9 `datetime.strptime` |
| 空数据 raise | 各 `_query_*` 函数 |
| server.py 两处改动 | Task 12 |
| test_server 断言 4→6 | Task 12 |
| README 更新 | Task 14 |
| SKILL.md 更新 | Task 15 |
| 2180495.IB 回归 case | Task 3 Step 1 |
| 113682 益丰转债(上市发行人) | Task 10 验收标准对齐(spec 验收第3条) |

**2. Placeholder scan:** 无 TBD/TODO/「类似 Task N」引用。每个 code step 含完整代码。

**3. Type consistency:**
- `query_bond(kind, file_path, start_date, end_date, bond_code, symbol)` — Task 2 定义,Task 12 调用一致 ✓
- `query_convertible_bond(kind, file_path, symbol, start_date, end_date, period, bond_code, stock_code, report_type, keyword)` — Task 7 定义,Task 12 调用一致 ✓
- `_normalize_bond_code` / `_normalize_cb_code` — 定义与调用一致 ✓
- `_query_yield_curve` / `_query_issue_info` / `_query_credit_daily` — 命名一致 ✓
- `_query_overview` / `_query_terms` / `_query_history` / `_query_issuer_finance` — 命名一致 ✓
- `_resolve_stock_code_from_cb` / `_build_nonlisted_hint` — Task 10 定义,测试中引用一致 ✓
- `BondKind` / `CbKind` / `ReportType` Literal — 定义一致 ✓

无遗漏,无类型不一致。
