# 金融输入归一化扩展:股票/标债反查 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「输入归一化」从转债扩展到股票与标债:新增 `resolve_stock_code`(名称→代码候选)tool,给 `query_bond(issue_info)` 加按发行人查(返回最新一只债代码),并升级 SKILL.md 为全品种归一化框架。

**Architecture:** 沿用现有 provider 模式。`resolve_stock_code` 放 `stock.py`,**首选新浪 suggest API**(支持全称反查,稳定)过滤类型11提取 `sh/sz+6位代码`,降级用 akshare `stock_zh_a_spot_em()` 全表按简称 contains;`bond.py` 的 `issue_info` 加 `bond_issue` 参数调 `bond_info_cm(bond_issue=...)` 按发行日期降序取最新一只;`server.py` 注册第 7 个 tool。转债不改代码(已有 `overview(keyword)`)。

**Tech Stack:** Python 3.10+、akshare(已装)、pytest。

---

## 设计依据与能力边界(实测确认)

### 股票反查 —— 多网站实测结果(2026-07-02)

实测 5 个财经网站搜索 API,用简称"茅台"+全称"贵州茅台酒股份有限公司"双测:

| 网站 | API | 简称 | 全称 | 稳定性 | 结论 |
|---|---|---|---|---|---|
| **新浪 suggest** | `suggest3.sinajs.cn/suggest/type=?key=` | ✅ 类型11 `sh600519` | ✅ 关联字段带 `sh600519\|全称\|权重` | ✅ 稳定200 | **首选** |
| 腾讯 smartbox | `smartbox.gtimg.cn/s3/?t=all&q=` | ✅ `sh~600519~贵州茅台` | ❌ `N` | ✅ 稳定 | 备选(仅简称) |
| 东财 searchapi | `searchapi.eastmoney.com/api/suggest/get` | ❌ 0 | ❌ 0 | ❌ 时有时无 | 弃用 |
| 同花顺 | `search.10jqka.com.cn/index?kw=` | 返回HTML非API | 同 | — | 弃用(需解析HTML) |
| 雪球 | `xueqiu.com/query/v1/suggest_stock.json` | 需登录(400016) | 同 | — | 弃用 |

**新浪 suggest 解析(GBK 编码,逗号分隔字段)**:
- 简称"茅台" → 1 条,类型`11`(股票),字段 `[名称,类型,代码,带前缀代码,简称,...]` → `sh600519`
- 全称"贵州茅台酒股份有限公司" → 7 条(混入持有该股的基金,类型`201`),但每条倒数第二字段含 `sh600519|贵州茅台酒股份有限公司|权重` → 从该字段提取 `sh600519`
- 城投"成都东方广益投资有限公司" → 0 条(正确,非上市)
- 简称"益丰药房" → 50 条(1股票类型11 + 49基金),股票行 `sh603939`

**`resolve_stock_code` 实现策略**:调新浪 suggest API,过滤类型=11(股票)的候选;若全称搜索无类型11结果,从关联字段(`sh/sz+6位代码|名称|权重`)提取去重。新浪失败时降级 akshare `stock_zh_a_spot_em()` 全表按简称 contains(仅简称有效)。

### 标债按发行人

akshare `bond_info_cm(bond_issue=发行人)` 已实测可用(查"成都东方广益"命中其全部债)。按「发行日期」降序取**最新一只**(用户决策:不返回全部列表,只返回最新一只代码供链路继续)。

### 转债

已有 `query_convertible_bond(overview, keyword=正股简称)`,不改代码。

## 环境关键约束

本项目 editable 安装指向别处,**所有 pytest 必须带 `PYTHONPATH=src` 前缀**,否则 `ModuleNotFoundError`。纯函数测试加 `SKIP_INTEGRATION=1`。`.gitignore` 第55行有 `tests/`(只影响未追踪新文件;已追踪 test 文件正常 add,新 test 文件若被拒用 `git add -f`)。

**新增依赖**:`resolve_stock_code` 用 `requests` 调新浪 suggest API。akshare 已依赖 requests(传递依赖),但为显式起见需确认 `pyproject.toml` 的 `dependencies` 含 `requests`;若无则添加(参考现有 `akshare`/`pandas` 写法)。

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/local_datasource/providers/stock.py` | 新增 `resolve_stock_code` + `_resolve_via_sina`(新浪suggest API,支持全称) | 修改(加函数,不改 `query_stock`) |
| `src/local_datasource/providers/bond.py` | `issue_info` 加 `bond_issue` 分支 + `_query_issue_info_by_issuer` | 修改 |
| `src/local_datasource/server.py` | 注册 `resolve_stock_code` tool + 路由 + import | 修改 |
| `pyproject.toml` | 显式声明 `requests` 依赖(新浪 API 用) | 修改 |
| `tests/test_stock.py` | `resolve_stock_code` 测试 | 修改 |
| `tests/test_bond.py` | `issue_info` by issuer 测试 | 修改 |
| `tests/test_server.py` | tool 数 6→7 | 修改 |
| `SKILL.md` | 全品种输入归一化框架 | 修改 |

---

## Task 1: `resolve_stock_code` — 名称→代码候选(TDD)

**Files:**
- Modify: `src/local_datasource/providers/stock.py`
- Test: `tests/test_stock.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_stock.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_resolve_stock_code_by_abbreviation():
    """简称反查:茅台 → 候选含 600519。"""
    from local_datasource.providers.stock import resolve_stock_code
    import pandas as pd
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = resolve_stock_code(keyword="茅台", file_path=path)
        df = pd.read_csv(file_path)
        assert len(df) >= 1
        assert "600519" in df["代码"].astype(str).values
    finally:
        os.unlink(path)


@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_resolve_stock_code_by_full_name():
    """全称反查:贵州茅台酒股份有限公司 → 命中 600519(从新浪关联字段提取)。"""
    from local_datasource.providers.stock import resolve_stock_code
    import pandas as pd
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = resolve_stock_code(keyword="贵州茅台酒股份有限公司", file_path=path)
        df = pd.read_csv(file_path)
        # 新浪全称搜索从关联字段提取 sh600519
        assert "600519" in df["代码"].astype(str).values
    finally:
        os.unlink(path)


@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_resolve_stock_code_non_listed_returns_empty():
    """城投/非上市发行人名称 → 候选为空(非报错)。"""
    from local_datasource.providers.stock import resolve_stock_code
    import pandas as pd
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = resolve_stock_code(keyword="成都东方广益投资有限公司", file_path=path)
        df = pd.read_csv(file_path)
        # 城投平台未上市,新浪搜不到 → 候选为空(不报错)
        assert len(df) == 0
    finally:
        os.unlink(path)


def test_resolve_stock_code_requires_keyword():
    """缺 keyword 应报错(纯函数)。"""
    from local_datasource.providers import stock
    with pytest.raises(ValueError, match="resolve_stock_code requires keyword"):
        stock.resolve_stock_code(keyword="", file_path="/tmp/x.csv")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH=src python -m pytest tests/test_stock.py -v -k resolve`
Expected: FAIL,`AttributeError: module ... has no attribute 'resolve_stock_code'` 或 `ImportError`

- [ ] **Step 3: 声明 requests 依赖**

`pyproject.toml` 的 `dependencies` 列表当前无 `requests`(akshare 传递依赖,但显式声明更稳)。在 `dependencies` 数组里加一行(按字母序插在 `pyyaml` 后):

```toml
    "requests>=2.28.0",
```

改后重装(确认 import 可用):
```bash
pip install -e .
```
(若 editable 安装指向别处导致 `pip install -e .` 报错,跳过此步——`requests` 已通过 akshare 传递可用,显式声明仅为发布规范)

- [ ] **Step 4: 写实现**

在 `src/local_datasource/providers/stock.py` 顶部 import 块加 `requests`:
```python
import requests
```
(若已有则跳过)

在 `src/local_datasource/providers/stock.py` 末尾(`query_stock` 之后)追加:

```python
def _resolve_via_sina(keyword: str) -> pd.DataFrame:
    """用新浪 suggest API 按名称反查股票代码(支持简称与全称)。

    新浪返回 GBK 编码,逗号分隔字段:[名称,类型,代码,带前缀代码,简称,...,关联字段]。
    - 类型 11 = 股票;简称搜索时直接命中类型11行
    - 全称搜索时,结果多为持有该股的基金(类型201),但每条关联字段含
      `sh600519|贵州茅台酒股份有限公司|权重`,从中提取 sh/sz+6位代码去重

    返回 DataFrame(代码,名称),无结果返回空 DataFrame。
    """
    try:
        r = requests.get(
            "https://suggest3.sinajs.cn/suggest/type=",
            params={"key": keyword},
            timeout=10,
            headers={"User-Agent": "Mozilla/5.0"},
        )
        r.encoding = "gbk"
    except Exception:
        return pd.DataFrame(columns=["代码", "名称"])

    import re
    m = re.search(r'suggestvalue="(.*)"', r.text, re.S)
    if not m or not m.group(1):
        return pd.DataFrame(columns=["代码", "名称"])

    rows = []
    seen = set()
    for cand in m.group(1).split(";"):
        if not cand:
            continue
        parts = cand.split(",")
        # parts[1]=类型, parts[2]=代码, parts[3]=带前缀代码, parts[4]=简称
        typ = parts[1] if len(parts) > 1 else ""
        prefix_code = parts[3] if len(parts) > 3 else ""
        name = parts[4] if len(parts) > 4 else ""
        # 类型11(股票)直接取
        if typ == "11" and prefix_code and prefix_code not in seen:
            seen.add(prefix_code)
            rows.append({"代码": re.sub(r"^(sh|sz)", "", prefix_code),
                         "名称": name})
        # 全称场景:从最后含 | 的关联字段提取 sh/sz+6位代码
        for field in parts:
            mm = re.search(r"\b(sh|sz)(\d{6})\|", field)
            if mm:
                full = mm.group(1) + mm.group(2)
                code = mm.group(2)
                if code not in seen:
                    seen.add(code)
                    rows.append({"代码": code, "名称": name})
    return pd.DataFrame(rows, columns=["代码", "名称"]) if rows else pd.DataFrame(columns=["代码", "名称"])


def resolve_stock_code(keyword: str, file_path: str) -> tuple[str, str]:
    """股票名称(简称或全称)→ 代码候选列表,输出 CSV。

    首选新浪 suggest API(支持全称反查);新浪失败或无结果时降级
    akshare stock_zh_a_spot_em() 全表按简称 contains 匹配(仅简称有效)。
    - 简称(如"茅台")→ 新浪类型11直接命中
    - 全称(如"贵州茅台酒股份有限公司")→ 新浪从关联字段提取代码
    - 城投/非上市发行人 → 候选为空(不报错)

    多候选时返回全部,Agent/用户从中选,再调 query_stock 查行情。
    """
    if not keyword:
        raise ValueError("resolve_stock_code requires keyword")

    # 首选新浪
    result = _resolve_via_sina(keyword)
    if result.empty:
        # 降级:akshare 全表按简称 contains
        try:
            df = ak.stock_zh_a_spot_em()
            if not df.empty:
                mask = df["名称"].astype(str).str.contains(keyword, na=False, regex=False)
                result = df[mask][["代码", "名称"]].copy()
        except Exception:
            pass
    return format_csv_output(result, file_path)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `PYTHONPATH=src python -m pytest tests/test_stock.py -v -k resolve`
Expected: 4 个 PASS(3 集成需联网 + 1 纯函数)。新浪 API 稳定,3 集成应 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/local_datasource/providers/stock.py pyproject.toml tests/test_stock.py
git commit -m "feat(stock): add resolve_stock_code for name->code lookup"
```

---

## Task 2: `query_bond(issue_info)` 加 `bond_issue` 按发行人查(TDD)

**Files:**
- Modify: `src/local_datasource/providers/bond.py`
- Test: `tests/test_bond.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_bond.py` 末尾追加:

```python
@pytest.mark.skipif(os.environ.get("SKIP_INTEGRATION"), reason="integration")
def test_query_bond_issue_info_by_issuer():
    """按发行人查:成都东方广益 → 返回最新一只债(1 行,含代码)。"""
    import pandas as pd
    with tempfile.NamedTemporaryFile(suffix=".csv", delete=False) as f:
        path = f.name
    try:
        file_path, summary = query_bond(
            kind="issue_info", bond_issue="成都东方广益", file_path=path)
        df = pd.read_csv(file_path)
        # 按发行日期降序取最新一只,应只有 1 行
        assert len(df) == 1, f"expected 1 row (latest), got {len(df)}"
        assert "成都东方广益" in str(df.iloc[0].values)
    finally:
        os.unlink(path)


def test_query_bond_issue_info_bond_code_and_issue_mutually_exclusive():
    """bond_code 与 bond_issue 同时给应报错(纯函数)。"""
    from local_datasource.providers import bond
    with pytest.raises(ValueError, match="互斥"):
        bond.query_bond(kind="issue_info", bond_code="2180495",
                        bond_issue="成都东方广益", file_path="/tmp/x.csv")


def test_query_bond_issue_info_requires_bond_code_or_issue():
    """issue_info 缺 bond_code 且 bond_issue 应报错(纯函数)。"""
    from local_datasource.providers import bond
    with pytest.raises(ValueError, match="issue_info requires"):
        bond.query_bond(kind="issue_info", file_path="/tmp/x.csv")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH=src python -m pytest tests/test_bond.py -v -k "by_issuer or mutually_exclusive or requires_bond_code_or_issue"`
Expected: 第一个 FAIL(`TypeError: query_bond() got unexpected keyword 'bond_issue'`),后两个 FAIL(参数校验未实现)

- [ ] **Step 3: 写实现**

3a. 在 `src/local_datasource/providers/bond.py` 的 `_query_issue_info` 之后追加新函数:

```python
def _query_issue_info_by_issuer(issuer: str) -> pd.DataFrame:
    """按发行人查,返回最新一只标债(按发行日期降序取首条)。

    用户场景:给发行人名 → 拿到一个债券代码 → 链路继续查债信息/财务。
    故只返回最新一只,不返回全部列表。
    """
    df = ak.bond_info_cm(bond_issue=issuer)
    if df.empty:
        raise ValueError(f"No bond found for issuer: {issuer}")
    # 发行日期降序,取最新一只
    df = df.sort_values("发行日期", ascending=False).head(1).copy()
    return df
```

3b. 修改 `query_bond` 函数签名与 `issue_info` 分支。把:

```python
def query_bond(
    kind: BondKind,
    file_path: str,
    start_date: str | None = None,
    end_date: str | None = None,
    bond_code: str | None = None,
    symbol: str | None = None,
) -> tuple[str, str]:
    """查询中国境内债券数据并输出 CSV。"""
    if kind == "yield_curve":
        if not start_date or not end_date:
            raise ValueError("yield_curve requires start_date and end_date")
        df = _query_yield_curve(start_date, end_date)
    elif kind == "issue_info":
        if not bond_code:
            raise ValueError("issue_info requires bond_code")
        df = _query_issue_info(bond_code)
    elif kind == "credit_daily":
```

改为(加 `bond_issue` 参数 + 互斥校验 + 分支):

```python
def query_bond(
    kind: BondKind,
    file_path: str,
    start_date: str | None = None,
    end_date: str | None = None,
    bond_code: str | None = None,
    symbol: str | None = None,
    bond_issue: str | None = None,
) -> tuple[str, str]:
    """查询中国境内债券数据并输出 CSV。"""
    if kind == "yield_curve":
        if not start_date or not end_date:
            raise ValueError("yield_curve requires start_date and end_date")
        df = _query_yield_curve(start_date, end_date)
    elif kind == "issue_info":
        if bond_code and bond_issue:
            raise ValueError("issue_info: bond_code 与 bond_issue 互斥")
        if bond_code:
            df = _query_issue_info(bond_code)
        elif bond_issue:
            df = _query_issue_info_by_issuer(bond_issue)
        else:
            raise ValueError("issue_info requires bond_code or bond_issue")
    elif kind == "credit_daily":
```

(后续 `credit_daily` 分支不变)

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONPATH=src python -m pytest tests/test_bond.py -v -k "by_issuer or mutually_exclusive or requires_bond_code_or_issue"`
Expected: 3 个 PASS

- [ ] **Step 5: 提交**

```bash
git add src/local_datasource/providers/bond.py tests/test_bond.py
git commit -m "feat(bond): add bond_issue param to issue_info for issuer->latest bond"
```

---

## Task 3: server.py 注册 `resolve_stock_code`(TDD)

**Files:**
- Modify: `src/local_datasource/server.py`
- Test: `tests/test_server.py`

- [ ] **Step 1: 写失败测试**

把 `tests/test_server.py` 的 `test_build_tools_count` 与 name 集合更新(6→7):

```python
def test_build_tools_count():
    tools = build_tools()
    assert len(tools) == 7
    names = {t.name for t in tools}
    assert names == {
        "query_stock", "query_yfinance", "query_worldbank", "query_arxiv",
        "query_bond", "query_convertible_bond", "resolve_stock_code",
    }


def test_resolve_stock_code_schema():
    """resolve_stock_code 的 inputSchema 含 keyword + file_path。"""
    tools = {t.name: t for t in build_tools()}
    schema = tools["resolve_stock_code"].inputSchema
    assert "keyword" in schema["properties"]
    assert "file_path" in schema["properties"]
    assert set(schema["required"]) == {"keyword", "file_path"}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH=src python -m pytest tests/test_server.py -v`
Expected: FAIL(`len(tools) == 6` 断言失败,实际仍 6)

- [ ] **Step 3: 改 server.py — 加 import**

当前 `src/local_datasource/server.py` import 块有:
```python
from local_datasource.providers.stock import query_stock
```
改为:
```python
from local_datasource.providers.stock import query_stock, resolve_stock_code
```

同时更新文件顶部 docstring,把「注册 6 个数据查询 tool」改为「注册 7 个数据查询 tool」,并在 tool 列表(`query_stock` 那行后)加:
```
- ``resolve_stock_code``：股票名称（简称/全称）→ 代码候选
```

- [ ] **Step 4: 改 server.py — build_tools() 加 Tool**

在 `build_tools()` 的 `return [` 列表末尾(`query_convertible_bond` Tool 之后、`]` 之前)追加:

```python
        Tool(
            name="resolve_stock_code",
            description=(
                "Resolve A-share stock code by company name (abbreviation or full name). "
                "Output is written to file_path as CSV with candidate rows (代码+名称). "
                "简称精确命中;全称能命中简称子串则返回;城投/非上市发行人返回空候选。"
                "多候选时 Agent/用户从中选,再调 query_stock 查行情。"
            ),
            inputSchema={
                "type": "object",
                "properties": {
                    "keyword": {"type": "string", "description": "Stock name or keyword, e.g. 茅台 / 贵州茅台酒股份有限公司"},
                    "file_path": {"type": "string", "description": "Output CSV file path"},
                },
                "required": ["keyword", "file_path"],
            },
        ),
```

- [ ] **Step 5: 改 server.py — handle_call_tool() 加路由**

在 `handle_call_tool()` 的 elif 链里,`elif name == "query_convertible_bond":` 之后、`else:` 之前,插入:

```python
        elif name == "resolve_stock_code":
            _, summary = resolve_stock_code(**arguments)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `PYTHONPATH=src python -m pytest tests/test_server.py -v`
Expected: 3 个 PASS(count + bond schema + cb schema + 新 resolve schema;共 4 个测试函数,注意原来 3 个 + 新增 1 个 = 4)

- [ ] **Step 7: 提交**

```bash
git add src/local_datasource/server.py tests/test_server.py
git commit -m "feat(server): register resolve_stock_code tool (7 tools)"
```

---

## Task 4: 全量回归 + 冒烟

**Files:**
- Verify: 全项目

- [ ] **Step 1: 全量测试**

Run: `PYTHONPATH=src python -m pytest tests/ -v`
Expected: 全部 PASS(原有 + 新增)。集成失败时 `SKIP_INTEGRATION=1 PYTHONPATH=src python -m pytest tests/ -v` 跑纯函数子集应全 PASS。

- [ ] **Step 2: 冒烟验证**

Run:
```bash
PYTHONPATH=src python -c "
from local_datasource.server import build_tools
from local_datasource.providers.stock import resolve_stock_code
from local_datasource.providers.bond import query_bond
print('tools:', len(build_tools()))
p, s = resolve_stock_code(keyword='茅台', file_path='/tmp/smoke_resolve.csv')
print('resolve 茅台 ->', s.split(chr(10))[1])
p, s = query_bond(kind='issue_info', bond_issue='成都东方广益', file_path='/tmp/smoke_issuer.csv')
print('bond by issuer ->', s.split(chr(10))[1])
"
```
Expected: `tools: 7`;`resolve 茅台 -> Rows: >=1`(含 600519);`bond by issuer -> Rows: 1`

- [ ] **Step 3: 提交(空提交标记)**

```bash
git add -A
git commit -m "test: full regression after resolve_stock_code + bond_issue" --allow-empty
```

---

## Task 5: SKILL.md — 全品种输入归一化框架

**Files:**
- Modify: `SKILL.md`

- [ ] **Step 1: 工具选择速查表加 resolve_stock_code 行**

在 `SKILL.md`「二、工具选择速查」表格中,`query_convertible_bond` 行之后、「多资产归一化对比」行之前,插入:

```markdown
| 股票名称（简称/全称）→ 代码候选 | `resolve_stock_code` | 全表按简称匹配;多候选择一再调 query_stock |
```

- [ ] **Step 2: 新增「输入归一化总则」独立章节**

在「二、工具选择速查」章节之后(「三、各工具详细规范」之前)插入新章节:

```markdown
## 输入归一化总则（重要）

用户给金融标的时,可能贴代码、简称、全称或发行人名。Agent **调用 tool 前先按下表归一化输入**,不要把原始输入直接塞参数。

| 品种 | 用户输入 | Agent 动作 |
|---|---|---|
| 股票 | 公司简称（如茅台） | `resolve_stock_code(keyword=简称)` → 候选 → 选代码 → `query_stock` |
| 股票 | 公司全称（如贵州茅台酒股份有限公司） | `resolve_stock_code(keyword=全称)` → 命中简称子串则返回候选;空则提示用户提供简称 |
| 股票 | 已是代码（如 600519） | 直接 `query_stock` |
| 可转债 | 某公司转债 | `query_convertible_bond(kind=overview, keyword=正股简称)` → 该公司转债 |
| 可转债 | 转债代码 | 直接 `query_convertible_bond(kind=history, symbol=...)` |
| 标债 | 发行人名（如成都东方广益） | `query_bond(kind=issue_info, bond_issue=发行人)` → 最新一只债代码 → 继续查 |
| 标债 | 具体债代码/简称（如 2180495.IB） | `query_bond(kind=issue_info, bond_code=...)` |
| 发行人财务 | 任意债代码/发行人名 | 先归一化到代码 → `query_convertible_bond(kind=issuer_finance, ...)` |

**关键边界:**
- akshare 股票表只有简称无全称,全称反查靠简称子串匹配,命中不到属正常(城投/非上市发行人无股票)。
- 标债按发行人查只返回**最新一只**(按发行日期),非全部列表;需全部时让用户明确要求。
- 银行间债（.IB）/交易所信用债的发行人若为城投/非上市,`issuer_finance` 返回引导性提示（免费层无财务）。
```

- [ ] **Step 3: 更新 query_bond 章节 — issue_info 加 bond_issue 说明**

在 SKILL.md `query_bond` 章节的「可选参数」里,把 `bond_code` 行改为:

```markdown
- `bond_code`（issue_info 必填之一,如 `2180495.IB` / `2180495`,精确匹配单只）
- `bond_issue`（issue_info 必填之一,发行人名如 `成都东方广益`,与 `bond_code` 互斥;返回最新一只债）
```

并在「Agent 注意事项」加:
```markdown
- `issue_info` 的 `bond_code` 与 `bond_issue` 互斥;给发行人名时按发行日期降序返回最新一只债代码。
```

- [ ] **Step 4: 新增 resolve_stock_code 规范章节**

在 SKILL.md 末尾追加:

```markdown
## resolve_stock_code — 股票名称→代码

### 必填参数
- `keyword`：股票简称或全称（如 `茅台` / `贵州茅台酒股份有限公司`）
- `file_path`：输出 CSV 路径

### Agent 注意事项
- 返回候选列表（代码+名称）,多候选时择一再调 `query_stock`。
- 全称反查靠简称子串匹配;城投/非上市发行人返回空候选（正常,因其无上市股票）,应提示用户。
- akshare 股票表无全称字段,全称命中不到时建议用户提供简称。
```

- [ ] **Step 5: 提交**

```bash
git add SKILL.md
git commit -m "docs(skill): add 全品种输入归一化框架 + resolve_stock_code spec"
```

---

## Self-Review

**1. Spec coverage(对照方案):**
- 股票名称→代码反查 → Task 1
- 标债按发行人查最新一只 → Task 2
- server 注册第 7 tool → Task 3
- 全量回归 + 冒烟 → Task 4
- SKILL.md 全品种归一化 → Task 5
- 转债不改代码(已有 overview keyword)→ 无需 task ✓

**2. Placeholder scan:** 无 TBD/TODO。每个 code step 含完整代码,测试含完整断言。

**3. Type consistency:**
- `resolve_stock_code(keyword: str, file_path: str) -> tuple[str, str]` — Task 1 定义,Task 3 server 调用 `resolve_stock_code(**arguments)` 一致 ✓
- `query_bond(..., bond_issue: str | None = None)` — Task 2 加参数,Task 3 不动 bond 调用 ✓
- `_query_issue_info_by_issuer(issuer) -> pd.DataFrame` — Task 2 定义并调用 ✓
- tool 数 7、name 集含 `resolve_stock_code` — Task 3 测试与实现一致 ✓

无遗漏,无类型不一致。
